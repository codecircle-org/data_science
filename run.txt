#quickly serve all files and folders over HTTP using either a lightweight web server or Python’s built-in HTTP server.

jupyter lite build --contents content --output-dir dist
cd dist/
python3 -m http.server 8080 --bind 0.0.0.0
